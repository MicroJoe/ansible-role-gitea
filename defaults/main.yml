---
# defaults file for MicroJoe.gogs

gogs_group: git
gogs_user: git

gogs_version: 0.11.19
gogs_home: "/home/{{ gogs_user }}"
gogs_base: "{{ gogs_home }}/gogs"

gogs_db_user: gogs
gogs_db_password: gogs
gogs_db_name: gogs
